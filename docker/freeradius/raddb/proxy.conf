proxy server {
    default_fallback = no
}

# ローカルrealm（IdP）- 自機関の認証
realm kobedenshi.ac.jp {
    # ローカルで認証処理
}

# NULL realm（@なし）- 拒否
realm NULL {
    # realmなしのリクエストは拒否
}

# デフォルトrealm（SP）- 他機関のリクエストをFLRへ転送
# Phase 4で有効化
# home_server flr1 {
#     type = auth+acct
#     ipaddr = flr1.eduroam.jp
#     port = 1812
#     secret = ${FLR_SECRET}
#     response_window = 20
#     zombie_period = 40
#     revive_interval = 120
#     status_check = status-server
#     num_answers_to_alive = 3
# }
#
# home_server flr2 {
#     type = auth+acct
#     ipaddr = flr2.eduroam.jp
#     port = 1812
#     secret = ${FLR_SECRET}
#     response_window = 20
#     zombie_period = 40
#     revive_interval = 120
#     status_check = status-server
#     num_answers_to_alive = 3
# }
#
# home_server_pool eduroam_flr_pool {
#     type = fail-over
#     home_server = flr1
#     home_server = flr2
# }
#
# realm DEFAULT {
#     pool = eduroam_flr_pool
#     nostrip
# }
